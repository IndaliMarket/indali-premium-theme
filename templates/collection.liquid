<style>
:root {
    --primary: #f1c40f;
    --secondary: #f39c12;
    --dark: #2c3e50;
    --white: #ffffff;
    --shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    --shadow-lg: 0 10px 25px rgba(0, 0, 0, 0.15);
    --gradient: linear-gradient(135deg, #f1c40f 0%, #f39c12 100%);
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
    line-height: 1.6;
    color: var(--dark);
    background: #f8f9fa;
}

/* Hero Section */
.shop-hero {
    background: var(--gradient);
    color: var(--white);
    text-align: center;
    padding: 80px 20px;
    position: relative;
    overflow: hidden;
}

.shop-hero h1 {
    font-size: 56px;
    font-weight: 800;
    margin-bottom: 20px;
    text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
}

.shop-hero p {
    font-size: 22px;
    opacity: 0.95;
    max-width: 700px;
    margin: 0 auto 30px auto;
}

.hero-stats {
    display: flex;
    justify-content: center;
    gap: 50px;
    margin-top: 40px;
}

.hero-stat {
    text-align: center;
}

.hero-stat-number {
    font-size: 32px;
    font-weight: 800;
    display: block;
}

.hero-stat-label {
    font-size: 14px;
    opacity: 0.9;
    text-transform: uppercase;
    letter-spacing: 1px;
}

/* Categories Section */
.categories-section {
    background: var(--white);
    padding: 60px 0;
}

.section-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
}

.section-header {
    text-align: center;
    margin-bottom: 50px;
}

.section-title {
    font-size: 36px;
    font-weight: 800;
    color: var(--dark);
    margin-bottom: 15px;
}

.section-subtitle {
    font-size: 18px;
    color: #6c757d;
    max-width: 600px;
    margin: 0 auto;
}

.categories-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 30px;
}

.category-card {
    background: var(--white);
    border-radius: 20px;
    overflow: hidden;
    box-shadow: var(--shadow);
    transition: all 0.3s ease;
    position: relative;
    height: 300px;
    cursor: pointer;
}

.category-card:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow-lg);
}

.category-image {
    width: 100%;
    height: 200px;
    background: linear-gradient(45deg, var(--primary), var(--secondary));
    position: relative;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
}

.category-icon {
    font-size: 64px;
    color: var(--white);
    text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
}

.category-content {
    padding: 25px;
    text-align: center;
}

.category-title {
    font-size: 24px;
    font-weight: 700;
    color: var(--dark);
    margin-bottom: 8px;
}

.category-count {
    font-size: 14px;
    color: #6c757d;
    font-weight: 500;
}

.category-btn {
    position: absolute;
    bottom: 20px;
    left: 25px;
    right: 25px;
    background: var(--gradient);
    color: var(--white);
    border: none;
    padding: 12px;
    border-radius: 10px;
    font-weight: 600;
    cursor: pointer;
    transform: translateY(20px);
    opacity: 0;
    transition: all 0.3s ease;
}

.category-card:hover .category-btn {
    transform: translateY(0);
    opacity: 1;
}

/* Featured Products */
.featured-section {
    padding: 80px 0;
    background: #f8f9fa;
}

.products-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 30px;
    margin-top: 40px;
}

.product-card {
    background: var(--white);
    border-radius: 16px;
    overflow: hidden;
    box-shadow: var(--shadow);
    transition: all 0.3s ease;
    position: relative;
}

.product-card:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow-lg);
}

.product-image-container {
    position: relative;
    width: 100%;
    height: 220px;
    overflow: hidden;
    background: #f8f9fa;
}

.product-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.4s ease;
}

.product-card:hover .product-image {
    transform: scale(1.1);
}

.product-badge {
    position: absolute;
    top: 12px;
    left: 12px;
    background: #28a745;
    color: var(--white);
    padding: 6px 12px;
    border-radius: 15px;
    font-size: 12px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

.product-badge.sale { background: #e74c3c; }
.product-badge.new { background: var(--primary); }
.product-badge.popular { background: #3498db; }

.wishlist-btn {
    position: absolute;
    top: 12px;
    right: 12px;
    background: rgba(255, 255, 255, 0.9);
    border: none;
    padding: 10px;
    border-radius: 50%;
    cursor: pointer;
    transition: all 0.3s ease;
    font-size: 16px;
}

.wishlist-btn:hover {
    background: var(--white);
    transform: scale(1.1);
}

.product-info {
    padding: 24px;
}

.product-category {
    font-size: 12px;
    color: #6c757d;
    text-transform: uppercase;
    letter-spacing: 0.8px;
    font-weight: 600;
    margin-bottom: 8px;
}

.product-title {
    font-size: 18px;
    font-weight: 600;
    color: var(--dark);
    margin-bottom: 12px;
    line-height: 1.4;
    overflow: hidden;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    min-height: 50px;
}

.product-rating {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 12px;
}

.stars {
    color: #ffc107;
    font-size: 16px;
}

.rating-text {
    font-size: 14px;
    color: #6c757d;
}

.product-price {
    font-size: 24px;
    font-weight: 700;
    color: var(--primary);
    margin-bottom: 16px;
}

.product-price .original-price {
    font-size: 18px;
    color: #6c757d;
    text-decoration: line-through;
    margin-left: 10px;
    font-weight: 500;
}

.add-to-cart-btn {
    width: 100%;
    padding: 14px;
    background: var(--gradient);
    color: var(--white);
    border: none;
    border-radius: 10px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    font-size: 16px;
}

.add-to-cart-btn:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
}

/* Newsletter Section */
.newsletter-section {
    background: var(--dark);
    color: var(--white);
    padding: 60px 0;
    text-align: center;
}

.newsletter-content {
    max-width: 600px;
    margin: 0 auto;
}

.newsletter-title {
    font-size: 32px;
    font-weight: 800;
    margin-bottom: 15px;
}

.newsletter-text {
    font-size: 18px;
    opacity: 0.9;
    margin-bottom: 30px;
}

.newsletter-form {
    display: flex;
    gap: 15px;
    max-width: 400px;
    margin: 0 auto;
}

.newsletter-input {
    flex: 1;
    padding: 15px 20px;
    border: none;
    border-radius: 10px;
    font-size: 16px;
}

.newsletter-btn {
    background: var(--gradient);
    color: var(--white);
    border: none;
    padding: 15px 25px;
    border-radius: 10px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
}

.newsletter-btn:hover {
    transform: translateY(-2px);
}

/* Mobile Responsive */
@media (max-width: 768px) {
    .shop-hero h1 { font-size: 36px; }
    .shop-hero p { font-size: 18px; }
    .hero-stats { flex-direction: column; gap: 20px; }
    .categories-grid { grid-template-columns: 1fr; gap: 20px; }
    .products-grid { grid-template-columns: repeat(auto-fill, minmax(250px, 1fr)); gap: 20px; }
    .newsletter-form { flex-direction: column; max-width: 300px; }
}

@media (max-width: 480px) {
    .shop-hero { padding: 60px 20px; }
    .shop-hero h1 { font-size: 28px; }
    .products-grid { grid-template-columns: 1fr 1fr; gap: 15px; }
    .product-info { padding: 20px; }
    .section-title { font-size: 28px; }
    .category-card { height: 250px; }
    .category-image { height: 160px; }
}
</style>

<div class="shop-hero">
    <h1>Premium Shopping Experience</h1>
    <p>Discover thousands of quality products at everyday affordable prices with fast 3-day delivery</p>
    
    <div class="hero-stats">
        <div class="hero-stat">
            <span class="hero-stat-number">2000+</span>
            <span class="hero-stat-label">Products</span>
        </div>
        <div class="hero-stat">
            <span class="hero-stat-number">3 Days</span>
            <span class="hero-stat-label">Delivery</span>
        </div>
        <div class="hero-stat">
            <span class="hero-stat-number">5 Star</span>
            <span class="hero-stat-label">Service</span>
        </div>
    </div>
</div>

<section class="categories-section">
    <div class="section-container">
        <div class="section-header">
            <h2 class="section-title">Shop by Category</h2>
            <p class="section-subtitle">Browse our carefully curated categories to find exactly what you need</p>
        </div>

        <div class="categories-grid">
            <div class="category-card">
                <div class="category-image">
                    <div class="category-icon">🛒</div>
                </div>
                <div class="category-content">
                    <h3 class="category-title">Grocery Range</h3>
                    <p class="category-count">250+ Products</p>
                </div>
                <button class="category-btn">Browse Grocery</button>
            </div>

            <div class="category-card">
                <div class="category-image">
                    <div class="category-icon">🏠</div>
                </div>
                <div class="category-content">
                    <h3 class="category-title">Home Range</h3>
                    <p class="category-count">180+ Products</p>
                </div>
                <button class="category-btn">Browse Home</button>
            </div>

            <div class="category-card">
                <div class="category-image">
                    <div class="category-icon">⚡</div>
                </div>
                <div class="category-content">
                    <h3 class="category-title">Electronics</h3>
                    <p class="category-count">320+ Products</p>
                </div>
                <button class="category-btn">Browse Electronics</button>
            </div>

            <div class="category-card">
                <div class="category-image">
                    <div class="category-icon">🎮</div>
                </div>
                <div class="category-content">
                    <h3 class="category-title">Toys & Games</h3>
                    <p class="category-count">150+ Products</p>
                </div>
                <button class="category-btn">Browse Toys</button>
            </div>

            <div class="category-card">
                <div class="category-image">
                    <div class="category-icon">🔧</div>
                </div>
                <div class="category-content">
                    <h3 class="category-title">Tools & Hardware</h3>
                    <p class="category-count">200+ Products</p>
                </div>
                <button class="category-btn">Browse Tools</button>
            </div>

            <div class="category-card">
                <div class="category-image">
                    <div class="category-icon">⚽</div>
                </div>
                <div class="category-content">
                    <h3 class="category-title">Sports & Outdoors</h3>
                    <p class="category-count">120+ Products</p>
                </div>
                <button class="category-btn">Browse Sports</button>
            </div>
        </div>
    </div>
</section>

<section class="featured-section">
    <div class="section-container">
        <div class="section-header">
            <h2 class="section-title">Featured Products</h2>
            <p class="section-subtitle">Hand-picked products that our customers love most</p>
        </div>

        <div class="products-grid">
            {% for product in collections.all.products limit: 8 %}
            <div class="product-card">
                <div class="product-image-container">
                    {% if product.featured_image %}
                        <img src="{{ product.featured_image | image_url: width: 300, height: 220 }}" alt="{{ product.title }}" class="product-image">
                    {% else %}
                        <div style="background: #f8f9fa; display: flex; align-items: center; justify-content: center; height: 100%; color: #6c757d;">No Image</div>
                    {% endif %}
                    
                    {% if product.compare_at_price > product.price %}
                        <span class="product-badge sale">Sale</span>
                    {% elsif product.created_at > "now" | date: "%s" | minus: 2592000 %}
                        <span class="product-badge new">New</span>
                    {% else %}
                        <span class="product-badge popular">Popular</span>
                    {% endif %}
                    
                    <button class="wishlist-btn">♡</button>
                </div>
                <div class="product-info">
                    <div class="product-category">{{ product.type | default: "General" }}</div>
                    <h3 class="product-title">{{ product.title }}</h3>
                    <div class="product-rating">
                        <span class="stars">★★★★☆</span>
                        <span class="rating-text">(4.5) {{ product.id | modulo: 50 | plus: 10 }} reviews</span>
                    </div>
                    <div class="product-price">
                        {{ product.price | money }}
                        {% if product.compare_at_price > product.price %}
                            <span class="original-price">{{ product.compare_at_price | money }}</span>
                        {% endif %}
                    </div>
                    <form action="/cart/add" method="post">
                        <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}">
                        <button type="submit" class="add-to-cart-btn">Add to Cart</button>
                    </form>
                </div>
            </div>
            {% endfor %}
        </div>

        <div style="text-align: center; margin-top: 50px;">
            <a href="/collections/all" style="background: var(--gradient); color: white; padding: 16px 32px; border-radius: 12px; text-decoration: none; font-weight: 600; font-size: 18px; display: inline-block; transition: transform 0.3s ease;">
                View All Products →
            </a>
        </div>
    </div>
</section>

<section class="newsletter-section">
    <div class="newsletter-content">
        <h2 class="newsletter-title">Stay Updated</h2>
        <p class="newsletter-text">Get notified about new products, exclusive deals, and special offers</p>
        <form class="newsletter-form" action="#" method="post">
            <input type="email" class="newsletter-input" placeholder="Enter your email address" required>
            <button type="submit" class="newsletter-btn">Subscribe</button>
        </form>
    </div>
</section>

<script>
// Category card interactions
document.querySelectorAll('.category-card').forEach(card => {
    card.addEventListener('click', function() {
        const categoryTitle = this.querySelector('.category-title').textContent;
        const categorySlug = categoryTitle.toLowerCase().replace(/\s+/g, '-').replace(/&/g, '');
        window.location.href = `/collections/${categorySlug}`;
    });
});

// Add to cart functionality
document.querySelectorAll('.add-to-cart-btn').forEach(btn => {
    btn.addEventListener('click', function(e) {
        const originalText = this.textContent;
        this.textContent = 'Adding...';
        this.disabled = true;
        
        setTimeout(() => {
            this.textContent = '✓ Added!';
            this.style.background = '#27ae60';
            
            setTimeout(() => {
                this.textContent = originalText;
                this.style.background = '';
                this.disabled = false;
            }, 2000);
        }, 1000);
    });
});

// Wishlist functionality
document.querySelectorAll('.wishlist-btn').forEach(btn => {
    btn.addEventListener('click', function(e) {
        e.preventDefault();
        e.stopPropagation();
        
        const isActive = this.textContent === '♥';
        this.textContent = isActive ? '♡' : '♥';
        this.style.color = isActive ? '#333' : '#e74c3c';
        
        this.style.transform = 'scale(1.3)';
        setTimeout(() => {
            this.style.transform = 'scale(1)';
        }, 200);
    });
});

// Newsletter subscription
document.querySelector('.newsletter-form').addEventListener('submit', function(e) {
    e.preventDefault();
    
    const emailInput = this.querySelector('.newsletter-input');
    const submitBtn = this.querySelector('.newsletter-btn');
    const originalText = submitBtn.textContent;
    
    submitBtn.textContent = 'Subscribing...';
    submitBtn.disabled = true;
    
    setTimeout(() => {
        submitBtn.textContent = '✓ Subscribed!';
        submitBtn.style.background = '#27ae60';
        emailInput.value = '';
        
        setTimeout(() => {
            submitBtn.textContent = originalText;
            submitBtn.style.background = '';
            submitBtn.disabled = false;
        }, 2000);
    }, 1500);
});
</script>